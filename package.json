{
  "name": "ai-teaching-app",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "bash -lc 'cd backend && source .venv/bin/activate && uvicorn main:app --reload --port 8000'",
    "dev": "npm-run-all -p dev:frontend dev:backend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
